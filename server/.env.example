PORT=3001
# SQLite path. For Railway: use a Volume (e.g. mount at /data) and set SQLITE_PATH=/data/wedding.db so the DB persists across deploys.
SQLITE_PATH=./data/wedding.db
JWT_SECRET=your-secret-at-least-32-chars
MAIN_ADMIN_EMAIL=admin@example.com
MAIN_ADMIN_PASSWORD=changeme
# One-time seed: set this on Railway, then visit /api/admin/seed-main-admin?secret=THIS_VALUE once after first deploy.
# SEED_MAIN_ADMIN_SECRET=

# Optional: for AI-generated color palettes (Theme section "Generate from words")
OPENAI_API_KEY=sk-...

# Optional: for exact Google Maps link when admin selects a place. The frontend sends its VITE_GOOGLE_MAPS_API_KEY by default; set this only if you need a server-only key (e.g. no key on client).
# GOOGLE_PLACES_API_KEY=
